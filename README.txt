# ReadBinaryType

This is a Windows C# console application that takes as command line parameters a list of EXEs or DLLs and prints to standard output a CSV list of the filenames and their type (e.g., 32-bit vs 64.bit).
